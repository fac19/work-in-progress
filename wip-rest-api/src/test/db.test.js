const build = require('../database/build')
// const model = require('../model')

// beforeAll(() => {
//   build();
// });

// afterAll(() => {
//   build();
// });

// build();

describe("Databse test are running", () => {
  test("1 should equal 1", () => {
    
    expect(1).toEqual(1);

  });
});