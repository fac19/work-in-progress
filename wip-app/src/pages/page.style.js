import styled from "styled-components";

const MainWrapper = styled.main`
  margin: 5rem 0;
  text-align: center;
`;

const PageHeading = styled.h1`
  margin-bottom: 1em;
  font-size: 2em;
  color: rgba(0, 0, 0, 0.7);
`;

export { MainWrapper, PageHeading };
